/** @type {import('next').NextConfig} */
const nextConfig = {
	reactStrictMode: true,
	images: {
		domains: ['images.unsplash.com', 'cdn.simplehash.com', 'cdn.simplehash.com']
	  }	  
}

module.exports = nextConfig
